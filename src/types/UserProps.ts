// types/UserProps.ts

export interface User {
  username: string; // Adjust this based on your Firestore structure
  email: string; // Add other relevant user fields as needed
  // You can add more fields that are relevant to your user model
}
